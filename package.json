{
  "scripts": {
    "docker:compose:dev": "docker compose -f docker-compose.dev.yml up -d --force-recreate --build rpidaemon dev-db audiorecorderwindows",
    "docker:compose:prod": "docker compose -f docker-compose.prod.yml up -d --force-recreate --build rpidaemon audiorecorderlinux",
    "docker:compose:pull": "docker compose -f docker-compose.prod.yml pull",
    "docker:compose:stop": "docker compose stop"
  }
}
