{
  "scripts": {
    "docker:compose:dev": "docker compose -f docker-compose.dev.yml up -d --force-recreate --build rpidaemon dev-db",
    "docker:compose:prod": "docker compose -f docker-compose.prod.yml up -d --force-recreate --build rpidaemon",
    "docker:compose:stop": "docker compose stop"
  }
}
