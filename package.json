{
  "scripts": {
    "docker:build": "docker buildx build -t aopheim/budorbeach:rpidaemon --platform linux/arm --push .",
    "docker:compose:dev": "docker compose -f docker-compose.dev.yml up -d --force-recreate --build rpidaemon dev-db audiorecorderwindows",
    "docker:compose:prod": "sudo docker compose -f docker-compose.prod.yml up -d --force-recreate --build rpidaemon audiorecorderlinux",
    "docker:compose:pull": "sudo docker compose -f docker-compose.prod.yml pull",
    "docker:compose:stop": "sudo docker compose stop"
  }
}
