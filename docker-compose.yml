services:
  rpidaemon:
    build: .
    ports:
      - 80
    restart: unless-stopped
  birdnetserver:
    build:
      context: ../BirdNET-Analyzer
      dockerfile: Dockerfile
    ports:
      - 8080
